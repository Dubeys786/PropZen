package com.propzen.crm.model;

/**
 * Delivery and lifecycle status of communications.
 */
public enum CommunicationStatus {
    PENDING,
    QUEUED,
    SENT,
    DELIVERED,
    READ,
    FAILED,
    RECEIVED
}
